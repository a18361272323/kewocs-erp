---
title: "业务状态机"
tags: [业务, 状态机, 流程]
created: 2026-05-30
---

# 业务状态机

## 采购入库单

```mermaid
flowchart LR
    DRAFT[DRAFT 草稿] --> CONFIRMED[CONFIRMED 已确认]
    CONFIRMED --> CANCELLED[CANCELLED 已取消]
```

## 销售出库单

```mermaid
flowchart LR
    DRAFT[DRAFT 草稿] --> CONFIRMED[CONFIRMED 已确认]
    CONFIRMED --> OUT_STOCK[OUT_STOCK 已出库]
    OUT_STOCK --> PAID[PAID 已收款]
```

## 调拨单

```mermaid
flowchart LR
    DRAFT[DRAFT 草稿] --> CONFIRMED[CONFIRMED 已确认]
    CONFIRMED --> CANCELLED[CANCELLED 已取消]
```

## 盘点单

```mermaid
flowchart LR
    DRAFT[DRAFT 草稿] --> CONFIRMED[CONFIRMED 已完成]
```

## 退货单

```mermaid
flowchart LR
    DRAFT[DRAFT 草稿] --> CONFIRMED[CONFIRMED 已确认]
    CONFIRMED --> CANCELLED[CANCELLED 已取消]
```

## 相关笔记

- [[../业务/SN全生命周期]] — SN码状态流转
- [[模型速查]] — 各模型 modelKey
