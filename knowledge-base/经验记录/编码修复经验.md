---
title: "编码修复经验"
tags: [经验, 编码, UTF-8, Cloudflare, 排障]
---

# 编码修复经验

## 2026-05-29 Cloudflare 构建排障全程

### 症状1: 模块只编译21个 → 中文乱码

```
[vite:vue] Missing semicolon. (51:15)
```

**根因**: 之前用 Python `rb`/`wb` 编辑 .vue 文件时破坏了 UTF-8 多字节中文字符。

### 症状2: Element is missing end tag

```
src/views/Warehouse/Transfer.vue (32:9): Element is missing end tag
```

**根因**: `Out-File -NoNewline` 吞掉了所有换行符，文件变成一行。

### 症状3: Rollup failed to resolve import

```
Rollup failed to resolve import "/@api" from Dashboard.vue
```

**根因**: 路径写错，应为 `@/api`（Vite 别名）。

## 正确修复流程

> [!checklist] 标准修复步骤
> 1. `git show <commit>:<path>` 取原始干净文件
> 2. 用 PowerShell UTF-8 原生读写做修改
> 3. `[System.Text.UTF8Encoding]::new($false)` 无BOM写入
> 4. 全面扫描: `Get-Content -Raw | ? { $_ -match '\ufffd' }`
> 5. Push → 等 Cloudflare 构建验证

## 错误做法（避免）

- ❌ Python `open(file, 'rb')` / `open(file, 'wb')` 编辑 UTF-8 中文文件
- ❌ 猜测文件编码（GBK/UTF-16）后盲目转换
- ❌ `Out-File -NoNewline` 写 .vue 文件

## Git 推送配置

```bash
# Windows Git 需配置使用原生 OpenSSH
git config --global core.sshCommand 'C:/Windows/System32/OpenSSH/ssh.exe'
```

## 相关笔记

- [[Git工作流]] — 完整 Git 操作流程
- [[../项目架构/部署架构]] — Cloudflare 构建环境


## 相关文档

- [[../参考文档/索引]] — 项目所有参考文档索引
- [[../项目架构/项目README]] — 项目技术栈和结构

## 2026-05-30 模型方法地毯式审计

### 审计过程

对照 `docs/MODEL_API_DOCS.md`，逐模型逐方法检查：
1. 提取文档中全部 methodKey → parser 脚本生成清单
2. 与 `src/api/index.js` 108 个方法一对一对照 → 全部匹配
3. 逐字段核对新增/编辑方法入参 → 发现 5 项问题

### 发现问题与修复

| 严重度 | 文件 | 问题 | 修复 |
|--------|------|------|------|
| P0 | index.js | returnInDetailApi/returnOutDetailApi 未定义 | 新增两个 API 对象 |
| P1 | StockIn.vue | 发送 totalQuantity（入库主表无此字段） | 移除 |
| P1 | StockIn.vue | 发送 model/specification（SN码表无此字段） | 移除 |
| P1 | SnList.vue | doSnReturn 入参 snIds→id, returnReason→reason | 修正 |

### 平台字段类型调整

18 个金额/价格字段从 int 改为 decimal，详见 `docs/MODEL_API_DOCS.md` 附录。

### 教训

> [!important] 入参必须逐字段对照文档
> methodKey 匹配只是第一道防线。前端传的每个字段名、类型必须与模型定义完全一致。
> 多余的字段会被平台静默丢弃（不报错），缺少的字段会取默认值（常为null/0），
> 导致"入库成功但字段为空"这类不易排查的问题。